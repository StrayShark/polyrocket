strict-peer-dependencies=false
auto-install-peers=true

# pnpm 11.5 — use `allowBuilds` (not `onlyBuiltDependencies`)
allowBuilds[better-sqlite3]=true
allowBuilds[esbuild]=true
allowBuilds[@parcel/watcher]=true